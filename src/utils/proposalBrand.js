// RGB equivalents of the canonical Tailwind brand and accent tokens.
export const PROPOSAL_BRAND = Object.freeze({
  ink: Object.freeze([15, 23, 42]), // brand-900
  muted: Object.freeze([71, 85, 105]), // brand-500
  neutral: Object.freeze([248, 250, 252]), // brand-50
  border: Object.freeze([203, 213, 225]), // brand-200
  divider: Object.freeze([226, 232, 240]), // brand-100
  accent: Object.freeze([107, 142, 35]), // accent-500
  accentStrong: Object.freeze([74, 100, 24]), // accent-700
  white: Object.freeze([255, 255, 255]),
});
